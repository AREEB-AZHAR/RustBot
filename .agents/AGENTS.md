# Workspace Guidelines

## Workflow Rules
- Always commit changes to git after completing and reviewing work.

## Security Rules
- Never compromise security.
- Do not store API keys or secrets in browser `localStorage`. Rely strictly on server-side `.env` configuration or transient in-memory state.
